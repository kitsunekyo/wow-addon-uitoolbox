# Blizzard Button Templates Reference

A comprehensive reference of button templates and visual styles in the Blizzard WoW UI (retail only).
All templates are `virtual="true"` and available to addon authors via XML `inherits=""` or Lua `CreateFrame`.

